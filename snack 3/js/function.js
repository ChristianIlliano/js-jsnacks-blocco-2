/**
 * Description funzione che conta gli elementi di un determinato array
 * @returns {string}
 */
function contaElementi(arrayFunction) {

let result = 0;
for (let i = 0; i < arrayFunction.length; i++) {
result += 1;
}
return result
}