/**
 * Description
 * @param {string} word1
 * @param {string} word2
 * @returns {Boolean}
 */
function longestWord(word1, word2) {

    let longestLength = false;

    // for (let i = 0 - 1; i < word1.length + 1; i++) {
    //     console.log(word);
    // }

    if (word1.length = word2.length) {
        console.log(word1, word2);
    }
}