/**
 * Description
 * @param {string} array
 * @returns {string}
 */
function stampa(array) {
    let result = "";
    for (i = 0; i < array.length; i++) {
    result += array[i] + ","
    }
    return result
}